## RBSA.py 代码分析

### 一、研究目标

本代码实现了一套基于**滚动回归风格分析（Rolling-window Returns-Based Style Analysis, RBSA）**的基金研究框架，核心问题是：**主动权益型基金的成长/价值风格判断，能否预测未来4周的超额收益？** 换言之，研究"踩对风格方向"的基金经理是否真的能获得更高回报。

---

### 二、数据输入与关键参数

**数据来源（4个输入文件）：**
- `growth_index.xlsx`：国证成长指数日度收盘价
- `value_index.xlsx`：国证价值指数日度收盘价
- `GC007_加权平均.xlsx`：银行间回购利率 GC007（作为现金收益代理）
- `主动权益型基金净值变化.xlsx`：基金净值宽表（基金×日期矩阵）

**核心参数：**
| 参数 | 默认值 | 含义 |
|---|---|---|
| `WINDOW_WEEKS` | 52周 | 滚动回归窗口（约1年） |
| `STEP_WEEKS` | 4周 | 每次向前滚动步长（约1个月） |
| `MIN_FUND_WEEKS` | 104周 | 基金最短历史要求（约2年，用于过滤短命基金） |
| `MIN_R2` | 0.70 | RBSA回归最低拟合优度，低于此阈值的估计结果被标记为无效 |
| `BENCHMARK_COST_RATE` | 0.001 | 基准组合半年度再平衡的单边交易成本率（0.1%） |

---

### 三、分析流程（逐步拆解）

**Step 1：数据处理**
- 所有日度数据统一转换为**周频（每周五收盘）**
- GC007年化利率除以100再除以52，得到单周现金收益
- 基金净值经过零值剔除、去重、前向对齐处理后计算周收益率
- 四类序列取交集对齐日期，确保因子与基金收益完全同步

**Step 2：滚动 RBSA 回归**

对每只基金，在每个 52 周滚动窗口内，求解以下约束优化：

```
min Σ(r_fund - w_growth·r_growth - w_value·r_value - w_cash·r_cash)²
    s.t. w_growth, w_value, w_cash ≥ 0
         w_growth + w_value + w_cash ≤ 1
```

初始点为 (0.45, 0.45, 0.10)，失败后换用均匀分布点 (1/3, 1/3, 1/3) 重试。输出每只基金在每个时间点的 **三因子暴露权重** 及 **R²**。

并计算 `net_growth_exposure = w_growth - w_value`，作为衡量该基金"成长偏向程度"的核心指标。

**Step 3：分组构建**

在每个估计日，对所有 R² ≥ 0.70 的有效基金按 `net_growth_exposure` 降序排列：
- **成长偏向组**：前 1/3 分位的基金（净成长暴露最高）
- **价值偏向组**：后 1/3 分位的基金（净成长暴露最低）
- 中间 1/3 不纳入分析

**Step 4：风格判断与"踩对/踩错"标记**

以估计日后的 4 周为持有期，分别计算国证成长和国证价值指数的累计收益：
- 若成长 > 价值 → 当期主导风格为"成长"，成长偏向组为"**踩对组**"
- 若价值 ≥ 成长 → 当期主导风格为"价值"，价值偏向组为"**踩对组**"

**Step 5：基准组合（50/50静态再平衡）**

构建成长/价值各50%、每年6月底和12月底再平衡的等权基准，再平衡时扣除0.1%单边交易成本，作为全文的对照基准线。

**Step 6：稳定上榜基金筛选**

对全样本期，筛选满足以下条件的基金：
- **稳定上榜基金**（`is_stable_selected`）：被选入任意分组的次数 ≥ 25次
- **稳定超额基金**（`is_stable_outperform`）：被选次数 ≥ 10次 **且** 踩对率 ≥ 60% **且** 相对50/50基准的平均超额收益 > 0
- **动态风格基金**（`is_dynamic_style`）：在稳定上榜基金中，平均绝对净成长暴露 ≥ 60%分位数 **且** 暴露P90-P10变化幅度 ≥ 75%分位数（即风格切换幅度大且方向感强的基金）

---

### 四、输出文件（CSV）

| 文件名 | 内容 |
|---|---|
| `rbsa_exposures.csv` | 每只基金×每个时间点的三因子暴露、R²、是否有效 |
| `period_group_returns.csv` | 每期踩对组/踩错组/基准的4周累计收益及差值 |
| `fund_period_returns.csv` | 每只基金每期的收益及相对基准超额 |
| `stable_fund_stats.csv` | 全样本基金的踩对率、平均超额、风格动态性等综合统计 |
| `benchmark_5050_nav.csv` | 50/50基准日度净值及再平衡明细 |
| `annual_return_diff.csv` | 分年度踩对/踩错组收益差统计 |
| `summary_stats.csv` | 全局关键指标汇总 |

---

### 五、输出图片（共7张）

| 图片文件名 | 内容 |
|---|---|
| `annual_return_diff.png` | 柱状图：分年度"踩对组-踩错组"收益差（%） |
| `annual_return_lines.png` | 折线图：踩对组与踩错组分年度收益率对比 |
| `cumulative_return_comparison.png` | 全样本累计净值曲线：踩对组 / 踩错组 / 稳定上榜基金 / 动态风格基金 |
| `cumulative_return_comparison_from_2018.png` | 同上，但从2018年1月1日起截取 |
| `annual_return_lines_stable.png` | 折线图：稳定上榜基金、动态风格基金、踩错组的分年度收益率三线对比 |
| `cumulative_stable_vs_wrong_5050.png` | 累计净值：方向错误组 vs 稳定上榜基金 vs 动态风格基金（从稳定基金首次出现的时间起算） |
| `style_exposure_dynamics_scatter.png` | 散点图：稳定上榜基金的平均绝对净成长暴露（X轴）vs 暴露变化幅度（Y轴），气泡大小=被选次数，区分普通稳定基金与动态风格基金，并标注60%/75%分位数阈值线 |